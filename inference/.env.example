INFERENCE_PORT=8000
INFERENCE_HOST=0.0.0.0

# Path to a folder that contains: final_farmer_credit_model.joblib, final_farmer_credit_label_encoder.joblib,
# feature_columns.json, model_metadata.json  (e.g. fin-agri-score/artifacts/output)
MODEL_DIR=../artifacts/output

# Comma-separated (browser or Postman to FastAPI)
CORS_ORIGINS=http://localhost:4000,http://127.0.0.1:4000,http://localhost:5173,http://127.0.0.1:5173

LOW_CLASS_THRESHOLD=0.19

FIN_AGRI_SCORE_MIN=300
FIN_AGRI_SCORE_MAX=850

RISK_BAND_LOW_MIN=700
RISK_BAND_MEDIUM_MIN=550
